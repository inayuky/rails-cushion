<%= form_with(url: login_path, local: true) do |f| %>
  <div>
    <%= f.label :email, "Eメール" %>
    <%= f.email_field :email %>
  </div>
  <div>
    <%= f.label :password, "パスワード" %>
    <%= f.password_field :password %>
  </div>
  <%= f.submit "ログイン" %>
<% end %>